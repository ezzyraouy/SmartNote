name: SmartNote
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  http: ^1.4.0             # requêtes HTTP vers backend ou API IA
  provider: ^6.1.0          # gestion d’état simple
  flutter_markdown: ^0.6.10 # affichage markdown
  file_picker: ^6.1.0       # pour ajout de fichiers (PDF, audio...)
  path_provider: ^2.1.0 
  flutter_secure_storage: ^8.0.0
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
